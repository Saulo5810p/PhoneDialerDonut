diff --git a/contacts/src/main/java/com/android/contacts/ContactsListActivity.java b/contacts/src/main/java/com/android/contacts/ContactsListActivity.java
index 1a52654..91b3695 100644
--- a/contacts/src/main/java/com/android/contacts/ContactsListActivity.java
+++ b/contacts/src/main/java/com/android/contacts/ContactsListActivity.java
@@ -1488,9 +1488,22 @@ public final class ContactsListActivity extends ListActivity
                 break;
 
             case MODE_STREQUENT:
+                // Corrigido: a aba Favoritos usa LIST_STREQUENT_ACTION -> MODE_STREQUENT
+                // (não MODE_STARRED). A query antiga usava o endpoint legado
+                // "contacts/strequent" (Contacts.CONTENT_URI + "strequent"), que no
+                // provider moderno só devolve colunas da tabela agregada de contatos
+                // (sem contact_id/number/type, que são colunas da view Phone/Data) -
+                // por isso a query quebrava com "Invalid column contact_id" e a aba
+                // ficava sempre vazia. Trocado pra Phone.CONTENT_URI (mesma base já
+                // usada por MODE_ALL_CONTACTS/MODE_FREQUENT), juntando favoritos
+                // (starred=1) e frequentes (times_contacted>0) na mesma consulta,
+                // com favoritos primeiro (changeCursor() já espera essa ordem pra
+                // desenhar o separador entre as duas seções).
                 mQueryHandler.startQuery(QUERY_TOKEN, null,
-                        Uri.withAppendedPath(Contacts.CONTENT_URI, "strequent"), STREQUENT_PROJECTION,
-                        null, null, null);
+                        Phone.CONTENT_URI, STREQUENT_PROJECTION,
+                        Phone.STARRED + "=1 OR " + Contacts.TIMES_CONTACTED + " > 0", null,
+                        Phone.STARRED + " DESC, " + Contacts.TIMES_CONTACTED + " DESC, "
+                                + getSortOrder(STREQUENT_PROJECTION));
                 break;
 
             case MODE_PICK_PHONE:
@@ -1555,14 +1568,19 @@ public final class ContactsListActivity extends ListActivity
             }
 
             case MODE_STREQUENT: {
+                // Mesma correção do startQuery(): base agora é Phone.CONTENT_URI
+                // (ou o filtro de nome dessa mesma view), não mais o endpoint
+                // legado "strequent" que não existe mais nesse formato.
                 Uri uri;
                 if (!TextUtils.isEmpty(filter)) {
-                    uri = Uri.withAppendedPath(Contacts.CONTENT_URI, "strequent/filter/"
-                            + Uri.encode(filter));
+                    uri = Uri.withAppendedPath(Phone.CONTENT_FILTER_URI, Uri.encode(filter));
                 } else {
-                    uri = Uri.withAppendedPath(Contacts.CONTENT_URI, "strequent");
+                    uri = Phone.CONTENT_URI;
                 }
-                return resolver.query(uri, STREQUENT_PROJECTION, null, null, null);
+                return resolver.query(uri, STREQUENT_PROJECTION,
+                        Phone.STARRED + "=1 OR " + Contacts.TIMES_CONTACTED + " > 0", null,
+                        Phone.STARRED + " DESC, " + Contacts.TIMES_CONTACTED + " DESC, "
+                                + getSortOrder(STREQUENT_PROJECTION));
             }
 
             case MODE_PICK_PHONE: {
